// Preload script for Electron (enables secure IPC if needed)
// This file is optional for basic setup, but useful for IPC best practices

window.addEventListener("DOMContentLoaded", () => {
  // Example: Expose a version API to the renderer
  // window.electronAPI = { version: process.versions.electron };
});
